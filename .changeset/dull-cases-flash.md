---
'@gitlab/ui': minor
---

GlBarChart: Add scoped slots for tooltip customization

Introduces `tooltip-title`, `tooltip-content` and `tooltip-value` scoped slots to allow consumers to customize the chart's tooltip.
