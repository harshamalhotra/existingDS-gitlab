---
'@gitlab/ui': minor
---

Add initial font-family, font-size, font-weight design tokens

Add `font.family`, `font.size`, and `font.weight` design tokens
Add `line-height` tokens to Tailwind config
