---
'@gitlab/ui': patch
---

Use semantic design color token for `pre` elements.

The previous color was the static `$gray-900`. Now it's `color.neutral.950`
(light mode), `color.neutral.0` (dark mode).
