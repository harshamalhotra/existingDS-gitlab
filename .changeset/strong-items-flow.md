---
'@gitlab/ui': minor
---

Use filter CSS property for popover, tooltips, and dropdowns drop shadow

Use `filter: drop-shadow(...)` CSS property instead of `box-shadow` to
include tip pseudo elements in popovers, tooltips, and dropdowns
