---
'@gitlab/ui': patch
---

Move gl-action-neutral-colors mixin to Tailwind utility

Remove `gl-action-neutral-colors` SCSS mixin
Update `@include` usage to `@apply`
U[date tailwind default config to relative paths
